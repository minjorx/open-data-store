# Display

Chart visualization configs.

## Config Format

JSON/YAML configs define:

- **type** — chart type (line/bar/pie/scatter/table etc)
- **style** — colors, size, theme
- **data** — data source reference (pointing to `../storage/` files)
