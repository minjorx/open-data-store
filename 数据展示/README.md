# 数据展示

图表可视化配置与展示。

## 图表配置格式

图表通过 JSON/YAML 配置文件定义，配置项包括：

- **type** — 图表类型（line/bar/pie/scatter/table 等）
- **style** — 样式配置（颜色、尺寸、主题等）
- **data** — 引用的数据来源（指向 `../数据存储/` 下的数据文件）

## 目录结构

```
数据展示/
├── README.md
├── 配置/           # 图表配置文件
└── 静态资源/       # 展示用的 HTML/JS
```
